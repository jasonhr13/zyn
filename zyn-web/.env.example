PORT=8080
ZYN_DATA_DIR=./data
# Optional extra lock in front of the Zyn account login. Leave empty for the usual path.
ZYN_WEB_TOKEN=
ZYN_SHAPE_TOKEN=
ZYN_SHAPE_PORT=4727
ZYN_ENGINE_PATH=
# Sign in at the web UI with the same Zyn email/password as desktop. This host is Full Engine;
# harvest-only desktops join its harvest room and mint both login and ATC cookies into the bank.
